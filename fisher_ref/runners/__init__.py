"""The campaign's runners: real CLIs that sweep a grid and write ``plan_exp_draft.md`` §13's format.

Distinct from ``experiments/``, which answer one protocol question each with printed numbers and no
CLI. Lot 2 ships ``p1_structural``; ``p2_operational`` is lot 5's.
"""
